# Example environment variables for local development
# Hugging Face API key (optional). If absent the server will run in MOCK mode.
HF_API_KEY=

# MongoDB connection string. If absent the server will use an in-memory store.
MONGO_URI=

# Server port
PORT=3000
